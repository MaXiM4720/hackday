server:
  port: 8080

spring:
  application:
    name: webrtc-signaling-server

logging:
  level:
    root: INFO
    com.example.signaling: DEBUG

app:
  cors:
    allowed-origins:
      - "http://localhost:3000"
      - "http://localhost:5173"
      - "http://127.0.0.1:3000"
      - "http://127.0.0.1:5173"